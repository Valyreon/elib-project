<UserControl x:Class="ElibWpf.Views.Controls.BooksTabControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ControlViews="clr-namespace:ElibWpf.Views.Controls"
             xmlns:ControlViewModels="clr-namespace:ElibWpf.ViewModels.Controls"
             xmlns:WindowViews="clr-namespace:ElibWpf.Views.Windows"
             xmlns:WindowViewModels="clr-namespace:ElibWpf.ViewModels.Windows"
             xmlns:MetroControls="http://metro.mahapps.com/winfx/xaml/controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <Style
                x:Key="ListBoxItemContainerStyle"
                TargetType="{x:Type ListBoxItem}"
                BasedOn="{StaticResource MetroListBoxItem}">
                <Setter Property="Background" Value="#222222" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                <Setter Property="VerticalContentAlignment" Value="Stretch" />
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="{StaticResource AccentColorBrush}" />
                    </Trigger>
                </Style.Triggers>
            </Style>

            <Style
                x:Key="ListBoxButtons"
                BasedOn="{StaticResource MetroListBox}"
                TargetType="{x:Type ListBox}">
                <Setter Property="Background" Value="#222222" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="ItemContainerStyle" Value="{StaticResource ListBoxItemContainerStyle}" />
            </Style>

            <Style x:Key="FlatButton" TargetType="{x:Type RadioButton}">
                <Setter Property="Background" Value="#222222" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="FontSize" Value="12" />
                <Setter Property="Padding" Value="0" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Height" Value="40"/>
                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type RadioButton}">
                            <StackPanel Orientation="Horizontal" x:Name="Canvas" Background="#222222">
                                <ContentPresenter
                                    HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                    VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                    RecognizesAccessKey="True" />
                            </StackPanel>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="Canvas" Property="Background" Value="#262626" />
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="Canvas" Property="Background" Value="#262626" />
                                    <Setter Property="Foreground" Value="White" />
                                </Trigger>
                                <MultiTrigger>
                                    <MultiTrigger.Conditions>
                                        <Condition Property="IsMouseOver" Value="True"/>
                                        <Condition Property="IsChecked" Value="False"/>
                                    </MultiTrigger.Conditions>
                                    <MultiTrigger.Setters>
                                        <Setter TargetName="Canvas" Property="Background" Value="#242424" />
                                    </MultiTrigger.Setters>
                                </MultiTrigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Foreground" Value="Gray" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <MetroControls:SplitView IsPaneOpen="{Binding IsPaneOpen ,UpdateSourceTrigger=PropertyChanged}" OpenPaneLength="230" CompactPaneLength="230" PaneBackground="#222222">
            <MetroControls:SplitView.Pane>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="40"></RowDefinition>
                        <RowDefinition Height="*"></RowDefinition>
                    </Grid.RowDefinitions>
                    <TextBox
                            MetroControls:TextBoxHelper.Watermark="Search..."
                            MetroControls:TextBoxHelper.ClearTextButton="True"
                            Grid.Row="0" Grid.Column="0"
                            VerticalContentAlignment="Center" Margin="5,0,5,0"></TextBox>

                    <Grid Grid.Row="1" Margin="10,5,10,5" Background="#222222">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"></RowDefinition>
                            <RowDefinition Height="30"></RowDefinition>
                            <RowDefinition Height="*"></RowDefinition>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"></ColumnDefinition>
                        </Grid.ColumnDefinitions>

                        <ListBox Grid.Row="0" Grid.Column="0" VerticalContentAlignment="Top" VerticalAlignment="Top" Foreground="Red" Background="#222222">
                            <ListBoxItem Style="{StaticResource ListBoxItemContainerStyle}">
                                <RadioButton
                                        Command="{Binding SelectPopularTab}"
                                        GroupName="Movies"
                                        IsChecked="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}, Path=IsSelected}"
                                        Style="{StaticResource FlatButton}">
                                    <Grid>
                                        <TextBlock
                                                    Margin="10,0,0,0"
                                                    VerticalAlignment="Center"
                                                    FontSize="16"
                                                    Text="All" />
                                    </Grid>
                                </RadioButton>
                            </ListBoxItem>
                            <ListBoxItem Style="{StaticResource ListBoxItemContainerStyle}">
                                <RadioButton
                                        Command="{Binding SelectPopularTab}"
                                        GroupName="Movies"
                                        IsChecked="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}, Path=IsSelected}"
                                        Style="{StaticResource FlatButton}">
                                    <Grid>

                                        <TextBlock
                                                    Margin="10,0,0,0"
                                                    VerticalAlignment="Center"
                                                    FontSize="16"
                                                    Text="Favorites" />
                                    </Grid>
                                </RadioButton>
                            </ListBoxItem>
                            <ListBoxItem Style="{StaticResource ListBoxItemContainerStyle}">
                                <RadioButton
                                        Command="{Binding SelectPopularTab}"
                                        GroupName="Movies"
                                        IsChecked="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}, Path=IsSelected}"
                                        Style="{StaticResource FlatButton}">
                                    <Grid>
                                        <TextBlock
                                                    Margin="10,0,0,0"
                                                    VerticalAlignment="Center"
                                                    FontSize="16"
                                                    Text="Read" />
                                    </Grid>
                                </RadioButton>
                            </ListBoxItem>
                            <ListBoxItem Style="{StaticResource ListBoxItemContainerStyle}">
                                <RadioButton
                                        Command="{Binding SelectPopularTab}"
                                        GroupName="Movies"
                                        IsChecked="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}, Path=IsSelected}"
                                        Style="{StaticResource FlatButton}">
                                    <Grid>
                                        <TextBlock
                                                    Margin="10,0,0,0"
                                                    VerticalAlignment="Center"
                                                    FontSize="16"
                                                    Text="Not Read" />
                                    </Grid>
                                </RadioButton>
                            </ListBoxItem>
                            <ListBoxItem Style="{StaticResource ListBoxItemContainerStyle}">
                                <RadioButton
                                        Command="{Binding SelectPopularTab}"
                                        GroupName="Movies"
                                        IsChecked="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}, Path=IsSelected}"
                                        Style="{StaticResource FlatButton}">
                                    <Grid>
                                        <TextBlock
                                                    Margin="10,0,0,0"
                                                    VerticalAlignment="Center"
                                                    FontSize="16"
                                                    Text="Series" />
                                    </Grid>
                                </RadioButton>
                            </ListBoxItem>
                            <ListBoxItem Style="{StaticResource ListBoxItemContainerStyle}">
                                <RadioButton
                                        Command="{Binding SelectPopularTab}"
                                        GroupName="Movies"
                                        IsChecked="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}, Path=IsSelected}"
                                        Style="{StaticResource FlatButton}">
                                    <Grid>
                                        <TextBlock
                                                    Margin="10,0,0,0"
                                                    VerticalAlignment="Center"
                                                    FontSize="16"
                                                    Text="Authors" />
                                    </Grid>
                                </RadioButton>
                            </ListBoxItem>
                        </ListBox>

                        <TextBlock
                                VerticalAlignment="Center"
                                Grid.Row="1"
                                Grid.Column="0"
                                FontSize="16"
                                Foreground="#637D8C"
                                Text="Collections"/>
                        <ScrollViewer Grid.Row="2" Grid.Column="0" VerticalScrollBarVisibility="Auto" Background="#222222">
                            <ListBox
                                    ItemsSource="{Binding Collections}"
                                    SelectedItem="{Binding SelectedCollection}"
                                Background="#222222"
                                ItemContainerStyle="{StaticResource ListBoxItemContainerStyle}">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <RadioButton
                                        Command="{Binding DataContext.ChangeCollection, ElementName=MenuBar}"
                                        CommandParameter="{Binding Id}"
                                        GroupName="PaneSelected"
                                        IsChecked="{Binding RelativeSource={RelativeSource AncestorType={x:Type ListBoxItem}}, Path=IsSelected}"
                                        Style="{StaticResource FlatButton}">
                                            <Grid>
                                                <TextBlock
                                                    Margin="10,0,0,0"
                                                    VerticalAlignment="Center"
                                                    FontSize="16"
                                                    Text="{Binding Tag}" />
                                            </Grid>
                                        </RadioButton>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </ScrollViewer>
                    </Grid>
                </Grid>
            </MetroControls:SplitView.Pane>
            <Grid Margin="0,5,0,0">

            </Grid>
        </MetroControls:SplitView>
    </Grid>
</UserControl>
